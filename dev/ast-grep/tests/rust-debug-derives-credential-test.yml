id: rust-debug-derives-credential
valid:
  - "#[derive(Debug)]\nstruct Poll { secret: bool }"
  - "#[derive(Clone)]\nstruct S { password: String }"
invalid:
  - "#[derive(Debug, Serialize)]\nstruct SignIn { email: String, password: String }"
