[package]
name = "tramp-agent"
version = "0.1.0"
authors = ["Niclas Overby <niclas@overby.me>"]
edition = "2024"
license = "MIT"
repository = "https://tangled.org/overby.me/overby.me/tree/main/nushell-plugin-tramp"
description = "Lightweight RPC agent for nushell-plugin-tramp remote filesystem operations"

[[bin]]
name = "tramp-agent"
path = "src/main.rs"

[dependencies]
rmp-serde = "1.3"
rmpv = { version = "1.3", features = ["with-serde"] }
# Serialization / MsgPack-RPC
serde = { version = "1", features = ["derive"] }

# Async runtime
tokio = { version = "1", features = [
  "fs",
  "io-std",
  "io-util",
  "macros",
  "net",
  "process",
  "rt-multi-thread",
  "signal",
  "sync",
  "time",
] }

# Filesystem watching (inotify/kqueue)
notify = "7"

# Native stat, uid/gid resolution
libc = "0.2"

# Error handling
thiserror = "2"

[dev-dependencies]
tempfile = "3"
