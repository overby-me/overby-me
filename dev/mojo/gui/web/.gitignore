__pycache__/
build/

# esbuild bundle artifact (generated by scripts/bundle-env.ts from
# runtime/browser.ts). It used to be a hand-written JavaScript port of
# runtime/env.ts and runtime/memory.ts kept beside them, and the two drifted.
examples/lib/env.js
