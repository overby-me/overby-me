# External tool library package
#
# Auto-discovered from external-tool/packages/lib.ncl.
# Because this is named "lib" inside a subworkspace named "external-tool",
# it appears in the flake outputs as packages.<system>.external-tool-lib
# (prefixed with the subworkspace directory name).
{
  description = "External tool shared library",
  build-system = "generic",
}
