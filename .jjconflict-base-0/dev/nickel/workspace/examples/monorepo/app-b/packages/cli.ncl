# app-b CLI package
#
# Auto-discovered from app-b/packages/cli.ncl.
# Because this is named "cli" inside a subworkspace named "app-b",
# it appears in the flake outputs as packages.<system>.app-b-cli
# (prefixed with the subworkspace directory name).
{
  description = "app-b command-line interface",
  build-system = "generic",
}
