{
	"lexicon": 1,
	"id": "wiki.radikal.nextSpeaker",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "The floor passes on: whoever had it leaves the queue, and the next turn's clock starts. An owner's. An empty queue is not an error.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["list_id"],
					"properties": {
						"list_id": {
							"type": "string"
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": [],
					"properties": {
						"served": {
							"type": "string",
							"description": "The entry that was served. Absent if the queue was empty."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such speaker list, or one in a context the caller is not a member of."
				},
				{
					"name": "Forbidden",
					"description": "The caller is not an owner of the context."
				}
			]
		}
	}
}
