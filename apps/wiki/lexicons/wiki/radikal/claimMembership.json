{
	"lexicon": 1,
	"id": "wiki.radikal.claimMembership",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Bind the invitation a `?claim=<token>` link names to the caller. This is how a rostered member, known only by an email address, becomes a DID. It binds and nothing more: voting rights stay as an owner set them. Repeating a claim the caller already made succeeds. A seat held by an account carried over from the interim counts as unclaimed: the link hands over that seat, and nothing else that account holds.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["token"],
					"properties": {
						"token": { "type": "string" }
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["context_id"],
					"properties": {
						"context_id": {
							"type": "string",
							"description": "The context the caller now belongs to."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "InvalidClaim",
					"description": "The token is unknown or no longer valid."
				},
				{
					"name": "AlreadyClaimed",
					"description": "Someone else has claimed this invitation."
				},
				{
					"name": "AlreadyMember",
					"description": "The caller already holds a membership of that context."
				}
			]
		}
	}
}
