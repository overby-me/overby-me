{
	"lexicon": 1,
	"id": "wiki.radikal.getContext",
	"defs": {
		"main": {
			"type": "query",
			"description": "Fetch a group/event context by id. Serves only what the caller may read.",
			"parameters": {
				"type": "params",
				"required": ["id"],
				"properties": {
					"id": { "type": "string", "description": "The context id." }
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": { "type": "ref", "ref": "wiki.radikal.defs#contextView" }
			},
			"errors": [
				{ "name": "NotFound", "description": "No context has that id." }
			]
		}
	}
}
