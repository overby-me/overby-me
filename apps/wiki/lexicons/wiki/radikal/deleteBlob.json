{
	"lexicon": 1,
	"id": "wiki.radikal.deleteBlob",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Delete a file. A member deletes what they uploaded; an owner of the context deletes anything in it. Nodes that still refer to the file are left as they are.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id"],
					"properties": {
						"id": {
							"type": "string"
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id"],
					"properties": {
						"id": {
							"type": "string"
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such file, or none the caller may read."
				},
				{
					"name": "Forbidden",
					"description": "The caller neither uploaded the file nor owns its context."
				}
			]
		}
	}
}
