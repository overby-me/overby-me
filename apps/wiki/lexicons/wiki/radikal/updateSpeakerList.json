{
	"lexicon": 1,
	"id": "wiki.radikal.updateSpeakerList",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "An owner renames a list, opens or closes it to new speakers, or sets the limit on a turn, which starts the clock afresh. Absent fields stay as they are.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id"],
					"properties": {
						"id": {
							"type": "string"
						},
						"name": {
							"type": "string"
						},
						"open": {
							"type": "boolean"
						},
						"turn_secs": {
							"type": "integer",
							"minimum": 0,
							"maximum": 86400
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["ok"],
					"properties": {
						"ok": {
							"type": "boolean"
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such speaker list, or one in a context the caller is not a member of."
				},
				{
					"name": "Forbidden",
					"description": "The caller is not an owner of the context."
				},
				{
					"name": "InvalidRequest",
					"description": "`turn_secs` is not between 0 and a day."
				}
			]
		}
	}
}
