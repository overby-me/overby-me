{
	"lexicon": 1,
	"id": "wiki.radikal.listSpeakerLists",
	"defs": {
		"main": {
			"type": "query",
			"description": "A context's speaker lists with their queues, for whoever may read the context: the room follows them on the projector. A queue is in the order it is served: the chair's override first, then the kind of contribution with a point of order ahead of a speech, then arrival. `now` is the server's clock, so a countdown does not depend on the viewer's.",
			"parameters": {
				"type": "params",
				"required": ["context"],
				"properties": {
					"context": {
						"type": "string"
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["lists", "profiles", "now"],
					"properties": {
						"lists": {
							"type": "array",
							"items": {
								"type": "ref",
								"ref": "#speakerListView"
							}
						},
						"profiles": {
							"type": "unknown",
							"description": "An object from DID to wiki.radikal.getNode#profileView, for every speaker in a queue."
						},
						"now": {
							"type": "string",
							"format": "datetime"
						}
					}
				}
			},
			"errors": [
				{
					"name": "NotFound",
					"description": "No such context, or none the caller may read."
				}
			]
		},
		"speakerListView": {
			"type": "object",
			"required": ["id", "context_id", "name", "open", "turn_secs", "queue"],
			"properties": {
				"id": {
					"type": "string"
				},
				"context_id": {
					"type": "string"
				},
				"name": {
					"type": "string"
				},
				"open": {
					"type": "boolean",
					"description": "Whether members may join."
				},
				"turn_secs": {
					"type": "integer",
					"description": "The limit on a turn in seconds. 0 runs no clock."
				},
				"turn_started_at": {
					"type": "string",
					"format": "datetime",
					"description": "When the current turn began, which the clock counts from."
				},
				"queue": {
					"type": "array",
					"description": "First is who has the floor.",
					"items": {
						"type": "ref",
						"ref": "#speakerEntryView"
					}
				}
			}
		},
		"speakerEntryView": {
			"type": "object",
			"required": ["id", "speaker_did", "kind", "created_at"],
			"properties": {
				"id": {
					"type": "string"
				},
				"speaker_did": {
					"type": "string",
					"format": "did"
				},
				"kind": {
					"type": "integer",
					"minimum": 0,
					"maximum": 4,
					"description": "0 a speech, 1 a question, 2 a clarification, 3 'I was misunderstood', 4 a point of order. A higher kind goes ahead."
				},
				"created_at": {
					"type": "string",
					"format": "datetime"
				}
			}
		}
	}
}
