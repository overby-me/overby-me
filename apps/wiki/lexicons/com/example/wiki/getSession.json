{
	"lexicon": 1,
	"id": "com.example.wiki.getSession",
	"defs": {
		"main": {
			"type": "query",
			"description": "Who the presented session belongs to. Requires authentication.",
			"output": {
				"encoding": "application/json",
				"schema": { "type": "ref", "ref": "com.example.wiki.defs#userView" }
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The request carries no session."
				},
				{
					"name": "InvalidToken",
					"description": "The session is unknown or has expired."
				}
			]
		}
	}
}
