{
	"lexicon": 1,
	"id": "com.example.wiki.moveDocument",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Move a document, and everything under it, to another parent, rewriting every path in it. It keeps its name if that is free there and takes the next one if not. Into ANOTHER context it takes an owner of both, and the comments on those documents, the files they hold and their closed polls change context with them, since each is read through its own. A running poll does not change groups: its voters are the members of where it was opened.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id", "parent_id"],
					"properties": {
						"id": {
							"type": "string"
						},
						"parent_id": {
							"type": "string",
							"description": "The new parent: a live context or document in the same context."
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["path"],
					"properties": {
						"path": {
							"type": "string",
							"description": "Its new path."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such document, or none the caller may read."
				},
				{
					"name": "Forbidden",
					"description": "The caller is not an owner of its context."
				},
				{
					"name": "InvalidRequest",
					"description": "The new parent is missing, in another context, the document itself, or somewhere inside it."
				}
			]
		}
	}
}
