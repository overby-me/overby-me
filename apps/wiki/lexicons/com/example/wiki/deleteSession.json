{
	"lexicon": 1,
	"id": "com.example.wiki.deleteSession",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Sign out: end the presented session and no other, so signing out on one device leaves the rest signed in. Requires authentication.",
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["ok"],
					"properties": {
						"ok": { "type": "boolean" }
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The request carries no session."
				},
				{
					"name": "InvalidToken",
					"description": "The session is unknown or has expired."
				}
			]
		}
	}
}
