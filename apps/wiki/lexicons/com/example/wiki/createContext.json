{
	"lexicon": 1,
	"id": "com.example.wiki.createContext",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Make a group or an event under a context, or under a folder in one, that the caller owns. It is its own context from the start: the caller is its first owner, with voting rights, and nobody else is in it. It starts closed. One step, where the interim needed four writes from the browser.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["kind", "name", "parent_id"],
					"properties": {
						"kind": {
							"type": "string",
							"enum": ["group", "event"]
						},
						"name": {
							"type": "string",
							"maxLength": 200
						},
						"parent_id": {
							"type": "string"
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id", "path"],
					"properties": {
						"id": {
							"type": "string"
						},
						"path": {
							"type": "string"
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such context, or none the caller may read."
				},
				{
					"name": "Forbidden",
					"description": "The caller does not own the context it would sit in."
				},
				{
					"name": "InvalidRequest",
					"description": "Not a group or an event, no name, or a parent that is neither a context nor a folder."
				}
			]
		}
	}
}
