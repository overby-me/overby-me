{
	"lexicon": 1,
	"id": "com.example.wiki.copyDocument",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Copy a document, with what the caller may read under it, to under another parent: in this context or in any other the caller owns. The copy is the caller's, keeps the original's date and author chips, and takes the next free name where it lands beside its source. A file it holds gets a file row of its own in the context it lands in, over the same bytes, so that it is readable there and deleting one copy leaves the other. Comments are not copied, and neither is a poll.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id", "parent_id"],
					"properties": {
						"id": {
							"type": "string"
						},
						"parent_id": {
							"type": "string",
							"description": "A document or a context to copy it under."
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id", "path", "copied"],
					"properties": {
						"id": {
							"type": "string"
						},
						"path": {
							"type": "string"
						},
						"copied": {
							"type": "integer",
							"description": "How many nodes the copy holds."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such document, or none the caller may read; or no such context."
				},
				{
					"name": "Forbidden",
					"description": "The caller does not own the context the copy would land in."
				},
				{
					"name": "InvalidRequest",
					"description": "No such parent, a parent this kind does not belong under, a parent inside what is being copied, a poll, or more than 2000 nodes."
				}
			]
		}
	}
}
