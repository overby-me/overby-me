{
	"lexicon": 1,
	"id": "com.example.wiki.restoreComment",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "Bring a comment back from the bin, as it was, with the reactions to it. Whoever could delete it may.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["id"],
					"properties": {
						"id": {
							"type": "string",
							"description": "The comment."
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["restored"],
					"properties": {
						"restored": {
							"type": "integer",
							"description": "How many comments came back: more than one for a thread the interim binned whole."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "Nothing in the bin by that id that the caller may restore."
				},
				{
					"name": "ParentInBin",
					"description": "The comment it answers is in the bin, or gone. Restore that first."
				}
			]
		}
	}
}
