{
	"lexicon": 1,
	"id": "com.example.wiki.notifyContext",
	"defs": {
		"main": {
			"type": "procedure",
			"description": "An owner tells the members of a context something, a poll having opened for one. It reaches the devices of members who hold voting rights and have accepted, and not the sender's. The words are the sender's client's. A device its push service reports gone is forgotten.",
			"input": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["context_id"],
					"properties": {
						"context_id": {
							"type": "string"
						},
						"title": {
							"type": "string",
							"description": "`RadikalWiki` if absent."
						},
						"body": {
							"type": "string"
						},
						"url": {
							"type": "string",
							"description": "What a tap opens: a path in the app, or a URL on a configured frontend origin. Anything else is refused."
						}
					}
				}
			},
			"output": {
				"encoding": "application/json",
				"schema": {
					"type": "object",
					"required": ["ok", "recipients", "sent"],
					"properties": {
						"ok": {
							"type": "boolean"
						},
						"recipients": {
							"type": "integer",
							"description": "Devices the message was sent towards."
						},
						"sent": {
							"type": "integer",
							"description": "Devices whose push service took it."
						}
					}
				}
			},
			"errors": [
				{
					"name": "AuthRequired",
					"description": "The caller is not authenticated."
				},
				{
					"name": "NotFound",
					"description": "No such context, or none the caller may read."
				},
				{
					"name": "Forbidden",
					"description": "The caller is not an owner of the context."
				},
				{
					"name": "InvalidRequest",
					"description": "A link that leads out of the app, or a message too long for a push."
				},
				{
					"name": "PushNotConfigured",
					"description": "This AppView has no VAPID key (HTTP 503)."
				}
			]
		}
	}
}
