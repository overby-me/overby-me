secrets/
# NTLM Type-3 message test vector (base64 of a deterministic handshake), not a
# credential.
safety/oxidized/curl/src/ntlm.rs
# An atproto DID document's verification key, which is public by definition.
apps/wiki/crates/spaces-spike/fixtures/commit.json
