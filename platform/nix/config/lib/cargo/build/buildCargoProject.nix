# Top-level API: build a Rust project from Cargo.lock with per-crate
# derivations. See PLAN.md and README.md.
{
  lib,
  stdenv,
  rustc,
  nushell,
  fetchurl,
  writeText,
  symlinkJoin,
  runCommand,
}: let
  cargoLib = import ../lib;
  buildCrate = import ./buildCrate.nix {inherit lib stdenv rustc nushell writeText;};
in
  {
    src,
    # Registry index checkout (snapshot mini-index or full index). When
    # null, the mini-index is generated by IFD from the sparse index for
    # exactly the crates this lock pins (see generatedIndex below): the
    # one opt-in exception to the library's IFD-free rule. Pass a committed
    # snapshot to stay fully pure.
    index ? null,
    # Set when the workspace manifest is not at the root of src (projects
    # with path dependencies on sibling directories).
    manifestDir ? "",
    lockFile ? null,
    pname ? null,
    # Version for the aggregate output of multi-root workspace builds
    # (single-root builds take the version from the crate manifest).
    version ? null,
    # Root package features.
    features ? [],
    noDefaultFeatures ? false,
    # Workspace members to build. Defaults to all members.
    roots ? null,
    # Subset of [[bin]] targets to build (names). null builds all.
    bins ? null,
    release ? true,
    # Cross-compilation (minimal): a cfg platform key like
    # "aarch64-linux". Normal deps resolve and compile for the target;
    # build scripts, their dependency closures, and proc-macros stay on
    # the host. Requires a toolchain carrying the target's std and a
    # crossCC for linking.
    crossTarget ? null,
    # Cross C compiler (e.g. pkgsCross.aarch64-multiplatform.stdenv.cc).
    crossCC ? null,
    # P7 (experimental): rmeta pipelining. Dependents compile against
    # dependency crate metadata so codegen leaves the critical path; bins
    # still link real rlibs. Requires a nightly toolchain with
    # -Zalways-encode-mir in rustcFlags: stable's standalone
    # --emit=metadata produces check-grade rmeta without the optimized
    # MIR dependents need for codegen.
    pipeline ? false,
    # Per-crate derivation attribute merges, keyed by crate name:
    # { openssl-sys = { buildInputs = [ openssl ]; nativeBuildInputs = [ pkg-config ]; }; }
    crateOverrides ? {},
    # Alternative linker package (e.g. pkgs.wild or pkgs.mold); its main
    # program is exposed as `ld` to cc via -B.
    linker ? null,
    # Extra flags for every rustc invocation, e.g.
    # ["-Zcodegen-backend=cranelift"] together with a nightly toolchain.
    rustcFlags ? [],
    # Toolchain override (e.g. rust-bin.nightly.latest.default with the
    # rustc-codegen-cranelift-preview extension).
    toolchain ? null,
    # Compile and run each root member's test targets (unit + tests/*.rs
    # integration tests, with dev-dependencies). Exposed as passthru.tests.<member>;
    # does not change the package's own derivations.
    runTests ? false,
    # Extra derivation attrs for the root crate (postInstall, env, ...).
    rootAttrs ? {},
    meta ? {},
  }: let
    inherit (builtins) attrNames concatStringsSep elem filter foldl' hashString head length listToAttrs mapAttrs readFile substring;

    hostPlatformDesc = cargoLib.cfg.platformFromSystem stdenv.hostPlatform.system;
    platform =
      if crossTarget != null
      then cargoLib.cfg.platformFromSystem crossTarget
      else hostPlatformDesc;
    workspace = cargoLib.manifest.loadWorkspace {inherit src manifestDir;};
    lockPath =
      if lockFile != null
      then lockFile
      else cargoLib.manifest.joinPath (cargoLib.manifest.joinPath src manifestDir) "Cargo.lock";
    lock = cargoLib.lock.parseLock (readFile lockPath);

    # IFD fallback for a missing index: reconstruct the mini-index from the
    # crates' published Cargo.toml, read out of the same fixed-output .crate
    # tarballs the build already fetches (hash from the lock). No network and
    # no sandbox relaxation: the tarballs are content-verified store paths, so
    # the derivation is pure and cacheable, and lib/index.nix reads its output
    # at eval time (the one IFD). Only forced when `index` is null, so passing
    # a committed snapshot keeps evaluation fully IFD-free. Keyed by the lock,
    # so it is shared across projects resolving the same crates.
    cratesIoPkgs =
      filter (p: p.sourceInfo.type == "registry" && (p.sourceInfo.cratesIo or false))
      lock.packages;
    indexManifest = writeText "cargo-index-manifest.json" (builtins.toJSON (map (p: {
        inherit (p) name version;
        cksum = p.checksum;
        tar = fetchCrate p;
      })
      cratesIoPkgs));
    generatedIndex =
      runCommand "cargo-index-${
        if pname != null
        then pname
        else "snapshot"
      }" {
        nativeBuildInputs = [nushell];
      } ''
        mkdir -p $out
        nu ${../tools/tarball-index.nu} $out ${indexManifest}
      '';

    effectiveIndex =
      if index != null
      then index
      else generatedIndex;

    rootNames =
      if roots != null
      then roots
      else map (m: m.name) workspace.members;

    singleRoot = length rootNames == 1;

    fetchCrate = pkg:
      fetchurl {
        name = "${pkg.name}-${pkg.version}.crate";
        url = "https://static.crates.io/crates/${pkg.name}/${pkg.name}-${pkg.version}.crate";
        sha256 = pkg.checksum;
      };

    # P5: the compiler cfg set is a pure function of the toolchain; run
    # `rustc --print cfg` once instead of in every build-script sandbox.
    rustcCfgFile =
      runCommand "rustc-cfg-${effectiveRustcVersion}" {
        nativeBuildInputs = [
          (
            if toolchain != null
            then toolchain
            else rustc
          )
        ];
      } ''
        rustc --print cfg ${
          if crossTarget != null
          then "--target ${platform.triple}"
          else ""
        } > $out
      '';

    resolved = cargoLib.resolve.resolve {
      inherit lock platform workspace;
      hostPlatform = hostPlatformDesc;
      indexDir = effectiveIndex;
      roots = rootNames;
      rootFeatures = features;
      inherit noDefaultFeatures;
    };
    inherit (resolved) nodes;

    profiles = cargoLib.profile.mkProfiles {
      inherit (workspace) rootManifest;
      inherit release;
    };

    linkerDir =
      if linker == null
      then null
      else
        runCommand "cargo-nix-ld" {} ''
          mkdir -p $out/bin
          ln -s ${lib.getExe linker} $out/bin/ld
        '';
    linkArgs =
      (
        if linker == null
        then []
        else ["-C" "link-arg=-B${linkerDir}/bin"]
      )
      ++ (
        if crossCC != null
        then ["-C" "linker=${crossCC}/bin/${crossCC.targetPrefix}cc"]
        else []
      );
    normalEdges = node: filter (e: e.kind == "normal") node.edges;
    buildEdges = node: filter (e: e.kind == "build") node.edges;

    # Transitive closure of normal deps per node (attrset of ids).
    closureSet =
      mapAttrs (
        _id: node:
          foldl' (
            acc: e:
              acc // {${e.targetId} = true;} // closureSet.${e.targetId}
          ) {} (normalEdges node)
      )
      nodes;

    # For the build script compile: build deps plus their normal closures.
    buildClosureSet =
      mapAttrs (
        _id: node:
          foldl' (
            acc: e:
              acc // {${e.targetId} = true;} // closureSet.${e.targetId}
          ) {} (buildEdges node)
      )
      nodes;

    effectiveRustcVersion =
      if toolchain != null
      then toolchain.version
      else rustc.version;

    hashOf = id: node:
      substring 0 16 (hashString "sha256"
        "${id}:${concatStringsSep "," node.features}:${effectiveRustcVersion}:v1");

    filterSrc = dir:
      builtins.filterSource (
        path: _type: let
          bn = baseNameOf path;
        in
          bn != "target" && bn != ".git" && bn != ".jj" && bn != "result"
      )
      dir;

    planFor = node: isRoot: {
      name = node.pkg.name;
      inherit (node.pkg) version;
      inherit (node.meta) edition links description license repository;
      authors = [];
      rustVersion = "";
      inherit (node.meta) lib;
      build = node.meta.buildScript;
      bins =
        if !isRoot
        then []
        else
          map (b: {inherit (b) name path;}) (filter (
              b:
                (bins == null || elem b.name bins)
                && (b.requiredFeatures == [] || builtins.all (f: elem f node.features) b.requiredFeatures)
            )
            node.meta.bins);
    };

    drvs = mapAttrs mkCrate nodes;

    # Host-platform variants for the build-dependency world (build
    # scripts and everything they link run on the compiling machine).
    # Native builds alias the main map so nothing changes.
    hostDrvs =
      if crossTarget == null
      then drvs
      else mapAttrs mkHostCrate nodes;

    mkHostCrate = id: node: let
      dedupeByName = edges:
        builtins.attrValues (foldl' (acc: e: acc // {${e.name} = e;}) {} edges);
    in
      buildCrate {
        crateName = "${node.pkg.name}-host";
        inherit (node.pkg) version;
        src =
          if node.pkg.sourceInfo.type != "registry"
          then filterSrc (cargoLib.manifest.joinPath (node.meta.srcBase or src) node.meta.relDir)
          else fetchCrate node.pkg;
        plan =
          if node.pkg.sourceInfo.type != "registry"
          then planFor node false
          else null;
        inherit (node) features;
        externs = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv = hostDrvs.${e.targetId};
        }) (dedupeByName (normalEdges node));
        buildExterns = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv = hostDrvs.${e.targetId};
        }) (dedupeByName (buildEdges node));
        depDrvs = map (i: hostDrvs.${i}) (attrNames closureSet.${id});
        buildDepDrvs = map (i: hostDrvs.${i}) (attrNames buildClosureSet.${id});
        linksDepDrvs = map (e: hostDrvs.${e.targetId}) (normalEdges node);
        target = hostPlatformDesc.triple;
        inherit rustcCfgFile;
        profile = profiles.forPackage node.pkg.name node.isWorkspaceMember;
        inherit rustcFlags toolchain;
        capLints = true;
        buildBins = false;
        crateHash = hashOf id node;
        extraAttrs = crateOverrides.${node.pkg.name} or {};
      };

    # Metadata-only derivations for pipelining. Members are excluded
    # (they are roots and build bins); everything else gets one, and the
    # sandbox decides eligibility (falling back to a full build for
    # proc-macros and build-script crates).
    rmetaDrvs =
      mapAttrs (
        id: node:
          if !pipeline || node.isWorkspaceMember
          then null
          else mkRmeta id node
      )
      nodes;

    # The derivation a lib compile should take its extern for `id` from.
    metaDrvOf = id:
      if rmetaDrvs.${id} != null
      then rmetaDrvs.${id}
      else drvs.${id};

    mkRmeta = id: node: let
      dedupeByName = edges:
        builtins.attrValues (foldl' (acc: e: acc // {${e.name} = e;}) {} edges);
    in
      buildCrate {
        crateName = "${node.pkg.name}-rmeta";
        inherit (node.pkg) version;
        src =
          if node.pkg.sourceInfo.type != "registry"
          then filterSrc (cargoLib.manifest.joinPath (node.meta.srcBase or src) node.meta.relDir)
          else fetchCrate node.pkg;
        plan =
          if node.pkg.sourceInfo.type != "registry"
          then planFor node false
          else null;
        inherit (node) features;
        emitMetadataOnly = true;
        externs = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv = metaDrvOf e.targetId;
        }) (dedupeByName (normalEdges node));
        buildExterns = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv = hostDrvs.${e.targetId};
        }) (dedupeByName (buildEdges node));
        depDrvs = map metaDrvOf (attrNames closureSet.${id});
        buildDepDrvs = map (i: hostDrvs.${i}) (attrNames buildClosureSet.${id});
        linksDepDrvs = map (e: metaDrvOf e.targetId) (normalEdges node);
        target = platform.triple;
        inherit rustcCfgFile;
        profile = profiles.forPackage node.pkg.name node.isWorkspaceMember;
        inherit linkArgs rustcFlags toolchain;
        capLints = true;
        buildBins = false;
        crateHash = hashOf id node;
        extraAttrs = crateOverrides.${node.pkg.name} or {};
      };

    mkCrate = id: node: let
      isRoot = node.isWorkspaceMember && elem node.pkg.name rootNames;
      dedupeByName = edges:
        builtins.attrValues (foldl' (acc: e: acc // {${e.name} = e;}) {} edges);
    in
      buildCrate {
        crateName = node.pkg.name;
        inherit (node.pkg) version;
        src =
          if node.pkg.sourceInfo.type != "registry"
          then filterSrc (cargoLib.manifest.joinPath (node.meta.srcBase or src) node.meta.relDir)
          else fetchCrate node.pkg;
        plan =
          if node.pkg.sourceInfo.type != "registry"
          then planFor node isRoot
          else null;
        inherit (node) features;
        externs = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv =
            if pipeline
            then metaDrvOf e.targetId
            else drvs.${e.targetId};
        }) (dedupeByName (normalEdges node));
        linkExterns =
          if !pipeline
          then null
          else
            map (e: {
              inherit (e) name;
              renamed = e.name != e.package;
              drv = drvs.${e.targetId};
            }) (dedupeByName (normalEdges node));
        linkDepOuts =
          if !pipeline
          then null
          else map (i: drvs.${i}) (attrNames closureSet.${id});
        fallbackFrom =
          if pipeline && !node.isWorkspaceMember
          then rmetaDrvs.${id}
          else null;
        emitMetadataOnly = false;
        buildExterns = map (e: {
          inherit (e) name;
          renamed = e.name != e.package;
          drv = hostDrvs.${e.targetId};
        }) (dedupeByName (buildEdges node));
        depDrvs = map (
          i:
            if pipeline
            then metaDrvOf i
            else drvs.${i}
        ) (attrNames closureSet.${id});
        buildDepDrvs = map (i: hostDrvs.${i}) (attrNames buildClosureSet.${id});
        linksDepDrvs = map (
          e:
            if pipeline
            then metaDrvOf e.targetId
            else drvs.${e.targetId}
        ) (normalEdges node);
        target = platform.triple;
        crossTarget =
          if crossTarget != null
          then platform.triple
          else null;
        hostTriple =
          if crossTarget != null
          then hostPlatformDesc.triple
          else null;
        inherit rustcCfgFile;
        profile = profiles.forPackage node.pkg.name node.isWorkspaceMember;
        inherit linkArgs rustcFlags toolchain;
        capLints = !node.isWorkspaceMember;
        buildBins = isRoot;
        crateHash = hashOf id node;
        extraAttrs =
          (crateOverrides.${node.pkg.name} or {})
          // lib.optionalAttrs (isRoot && singleRoot) ({
              pname =
                if pname != null
                then pname
                else node.pkg.name;
              inherit meta;
              passthru =
                {
                  crates = drvs;
                  inherit nodes;
                  tests = testRunners;
                }
                // (crateOverrides.${node.pkg.name}.passthru or {});
            }
            // rootAttrs);
      };

    # Test graph (only when runTests): a parallel, host-only, dev-inclusive
    # resolution and drv set, kept separate from the main graph so enabling
    # tests never alters the package's own derivations.
    resolvedTests =
      if !runTests
      then null
      else
        cargoLib.resolve.resolve {
          inherit lock workspace;
          platform = hostPlatformDesc;
          hostPlatform = hostPlatformDesc;
          indexDir = effectiveIndex;
          roots = rootNames;
          rootFeatures = features;
          inherit noDefaultFeatures;
          includeDev = true;
        };

    testRunners =
      if resolvedTests == null
      then {}
      else let
        tnodes = resolvedTests.nodes;
        dedupeByName = edges: builtins.attrValues (foldl' (acc: e: acc // {${e.name} = e;}) {} edges);
        tnormal = node: filter (e: e.kind == "normal") node.edges;
        tbuild = node: filter (e: e.kind == "build") node.edges;
        # Test targets link normal + dev deps.
        tcompile = node: filter (e: e.kind == "normal" || e.kind == "dev") node.edges;
        tClosure =
          mapAttrs (
            _id: node:
              foldl' (acc: e: acc // {${e.targetId} = true;} // tClosure.${e.targetId}) {} (tnormal node)
          )
          tnodes;
        tBuildClosure =
          mapAttrs (
            _id: node:
              foldl' (acc: e: acc // {${e.targetId} = true;} // tClosure.${e.targetId}) {} (tbuild node)
          )
          tnodes;
        tdrvs =
          mapAttrs (
            id: node:
              buildCrate {
                crateName = node.pkg.name;
                inherit (node.pkg) version;
                src =
                  if node.pkg.sourceInfo.type != "registry"
                  then filterSrc (cargoLib.manifest.joinPath (node.meta.srcBase or src) node.meta.relDir)
                  else fetchCrate node.pkg;
                plan =
                  if node.pkg.sourceInfo.type != "registry"
                  then planFor node false
                  else null;
                inherit (node) features;
                externs = map (e: {
                  inherit (e) name;
                  renamed = e.name != e.package;
                  drv = tdrvs.${e.targetId};
                }) (dedupeByName (tnormal node));
                buildExterns = map (e: {
                  inherit (e) name;
                  renamed = e.name != e.package;
                  drv = tdrvs.${e.targetId};
                }) (dedupeByName (tbuild node));
                depDrvs = map (i: tdrvs.${i}) (attrNames tClosure.${id});
                buildDepDrvs = map (i: tdrvs.${i}) (attrNames tBuildClosure.${id});
                linksDepDrvs = map (e: tdrvs.${e.targetId}) (tnormal node);
                target = hostPlatformDesc.triple;
                inherit rustcCfgFile;
                profile = profiles.forPackage node.pkg.name node.isWorkspaceMember;
                inherit rustcFlags toolchain;
                capLints = !node.isWorkspaceMember;
                buildBins = false;
                crateHash = hashOf id node;
                extraAttrs = crateOverrides.${node.pkg.name} or {};
              }
          )
          tnodes;
        mkRunner = id: let
          node = tnodes.${id};
          runClosure =
            foldl' (acc: e: acc // {${e.targetId} = true;} // tClosure.${e.targetId}) {} (tcompile node);
        in
          buildCrate {
            crateName = "${node.pkg.name}-tests";
            inherit (node.pkg) version;
            src = filterSrc (cargoLib.manifest.joinPath (node.meta.srcBase or src) node.meta.relDir);
            plan = planFor node true;
            inherit (node) features;
            externs = map (e: {
              inherit (e) name;
              renamed = e.name != e.package;
              drv = tdrvs.${e.targetId};
            }) (dedupeByName (tcompile node));
            buildExterns = map (e: {
              inherit (e) name;
              renamed = e.name != e.package;
              drv = tdrvs.${e.targetId};
            }) (dedupeByName (tbuild node));
            depDrvs = map (i: tdrvs.${i}) (attrNames runClosure);
            buildDepDrvs = map (i: tdrvs.${i}) (attrNames tBuildClosure.${id});
            linksDepDrvs = map (e: tdrvs.${e.targetId}) (tcompile node);
            target = hostPlatformDesc.triple;
            inherit rustcCfgFile;
            profile = profiles.forPackage node.pkg.name node.isWorkspaceMember;
            inherit rustcFlags toolchain;
            capLints = false;
            buildBins = false;
            crateHash = hashOf id node;
            testMode = true;
            extraAttrs = crateOverrides.${node.pkg.name} or {};
          };
      in
        listToAttrs (map (id: {
            name = tnodes.${id}.pkg.name;
            value = mkRunner id;
          })
          resolvedTests.rootIds);

    rootId = head resolved.rootIds;
  in
    if singleRoot
    then drvs.${rootId}
    else
      symlinkJoin ({
          name =
            if pname != null
            then pname
            else throw "buildCargoProject: set pname when building several workspace members";
          version =
            if version != null
            then version
            else "0.0.0";
          paths = map (i: drvs.${i}) resolved.rootIds;
          passthru = {
            crates = drvs;
            inherit nodes;
            tests = testRunners;
          };
          inherit meta;
        }
        // rootAttrs)
